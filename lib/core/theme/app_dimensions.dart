import 'package:flutter/material.dart';

class AppDimensions {
  // Global horizontal edge padding
  static const double horizontalPadding = 16.0;

  // Bottom Navigation Bar height
  static const double bottomNavBarHeight = 64.0;

  // Card heights and sizes
  static const double maxFormWidth = 480.0;
  static const double maxContentWidth = 600.0;

  // Aspect Ratios for visual consistency
  static const double aspectCardImage = 1.33; // 4:3
  static const double aspectBanner = 1.6;      // 8:5
  static const double aspectSquare = 1.0;     // 1:1

  // Elevation tokens
  static const double elevationNone = 0.0;
  static const double elevationLow = 1.0;
  static const double elevationMedium = 2.0;
  static const double elevationHigh = 4.0;
}
